counter = 0
secret = "9"
limit = 5

while counter < limit:
    guess = raw_input("What is the secret number? ")
    if guess == secret:
        print "Bingo! That's right!"
        break
    else:
        print "Nope, that's not the secret number."
    counter += 1

    print "Game Over :("
